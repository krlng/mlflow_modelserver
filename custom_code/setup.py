from setuptools import setup

setup(name='my_package',
      version='0.1',
      description='Custom functions for the model',
      license='MIT',
      packages=['my_package'],
      zip_safe=False)